server:
  port: 8080

spring:
  datasource:
    #url: jdbc:mysql://localhost:3306/enrollment?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    #url: jdbc:mysql://enrollment-db.c92y0sa8e7t2.ap-northeast-2.rds.amazonaws.com/enrollment_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    url: jdbc:mysql://enrollment-db.c92y0sa8e7t2.ap-northeast-2.rds.amazonaws.com/enrollment_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: choi
    password: 1q2w3e4r!!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false

auth-service:
  #url: http://localhost:8080
  url: http://auth-service.production.svc.cluster.local:8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
